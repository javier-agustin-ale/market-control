# Angular environment files are compiled at build time.
# Set src/environments/environment.ts apiUrl to your deployed API URL before deploying,
# or use a stable custom API domain.
API_URL=https://your-api-project.vercel.app/api
